{
  "buildCommand": "if [ ! -d \"flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && ./flutter/bin/flutter pub get && ./flutter/bin/flutter build web",
  "outputDirectory": "build/web",
  "framework": null
}
